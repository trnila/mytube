{block content}

<div class="panel panel-default">
	<div class="panel-heading">
		<h3 class="panel-title"><i class="fa fa-search"></i> Výsledky vyhledávání pro <strong>{$query}</strong> <span class="text-muted" n:if="$total > 0"> - {$total} výsledku</span></h3>
	</div>
	<div class="panel-body">
		{if count($videos)}
			{include ../_videos.latte}
		{else}
			Nic nenalezeno
		{/if}
	</div>
</div>